# 简历测试量化提示词

目标：让 DeepResearch 项目经历里的测试成果可量化、可复现、经得起面试官追问，同时避免把「原项目文档数字」写成「自己实测的数字」。

## 一、真实数据包（2026-08-08 从仓库 output/ 核实）

### 已实测

| 指标 | 数值 | 口径 / 来源 |
| --- | --- | --- |
| 内部端到端评测 | 10/10 通过，0 失败 | `output/eval_results/_summary.json`，`run_eval.py` 跑带标签查询集 |
| 意图路由准确率 | 100%（10/10） | `output/eval_metrics.json`，direct/multiagent 标签比对 |
| direct 分流占比 | 40%（4/10） | 同上 |
| 平均端到端耗时 | 58.8s（P50 93.8s，P95 102.6s） | 同上；注意是整条工作流耗时，不是检索单独耗时 |
| 平均报告字数 | 2680 字 | 同上 |
| 迭代补搜 | 平均 1.6 轮；50% 用例触发补搜；平均识别 1.9 个缺口 | 同上 |
| 完备性代理 | 33.3%（关键词覆盖口径，不是人工满意度） | `compute_metrics.py` |
| HTTP 黑盒冒烟 | 1/1 通过 | `output/eval_http_smoke/_summary.json`，`run_http_eval.py` 打部署后的 FastAPI |
| 并发压测冒烟 | 1 请求、并发 1、0 错误、平均 2.7s | `output/eval_stress_smoke.json`，`stress_test.py` |

### 来自原项目文档、当前仓库未实测

README 明确标注：96% 意图准确率、45%→12% 低质量信源、62%→89% 完备性、25%→6% 幻觉率、94% 引用准确率、检索耗时降 35%、双源检索 <8s、小时级→半小时内，均来自原项目文档，不是本仓库当前算出来的结果。

### 当前缺口（不建议直接写进简历，建议写成「测试发现的问题」）

- 引用统计 total=0：当前样例无法支撑「引用准确率 94%」。
- 低质量信源占比为空：无法支撑「45%→12%」。
- 人工评审表尚未生成（`output/eval_review.csv` 不存在）。
- 本地检索 raw=22 但 kept=0：需要先定位根因，再决定怎么写。
- `app/eval/sample_queries.jsonl` 只有 10 条：不要说「200 条内部测试集」。

## 二、提示词 A：让 AI 先核验并补跑真数

适合发给能直接读代码、跑命令的 AI（如 Codex）：

```
你是资深测试工程师兼简历顾问。项目在 D:\deepresearch\deep_research。
请先审计 app/eval、app/test、output 目录，然后完成：
1. 列出所有测试/评测资产（脚本、查询集、结果文件），说明每个脚本测什么、怎么跑。
2. 把真实结果整理成表：指标 | 数值 | 样本量 | 口径 | 来源文件 | 能否写进简历。
3. 如果环境允许，按 eval_run.ps1 依次跑 internal、metrics、review（--llm-judge）、http、stress，用最新 output 覆盖旧数字；跑不动就明确说没跑。
4. 输出一份《简历可写数字清单》，并单独列出「简历现有数字 vs 仓库实测数字」的差异。

硬性约束：
- 禁止把原项目文档里的目标值/设计值写成已达成。
- 样本量必须真实：10 条就写 10 条，禁止写 200 条。
- 引用准确率、幻觉率、低质量信源占比如果没测出来，就标「未测出」，不要估算。
- 发现真实结果与简历冲突（例如本地检索留存率为 0、引用为 0）时，写成「测试发现的问题」并给出下一步定位方向，不要隐藏。
```

## 三、提示词 B：量化改写简历

适合发给通用 AI（ChatGPT、Claude 等）。把「输入原文」和上面的「真实数据包」一起贴进去：

```
你是资深测试工程师 + 简历写作专家。请把下面的项目经历改写成「让面试官一眼看出候选人做过测试」的版本。

输入原文：
基于 LangGraph 多 Agent 协作构建企业级 AI 深度研究助手，实现从意图识别、双源检索、证据审计到带引用深度研报生成的端到端自动化。
基于 LangGraph + ResearchState 设计 8 个专家 Agent 流水线（意图路由、规划、双源侦察、证据裁判、分析、反思、撰稿），采用规则引擎 + LLM 双模态意图分流，准确率 96%，减少约 60% 无效深度研究调用。
搭建 Bocha 网络搜索 + Milvus 本地 RAG 双路并行检索与证据裁判机制，实现去重、冲突检测、信源评分，低质量信源占比从 45% 降至 12%；通过 Analyst + Reflect 迭代补搜闭环，研究完备性满意度从 62% 提升至 89%。
实现强制引用约束与正则校验，幻觉率从约 25% 降至 6%，引用准确率 94%；设计短期/长期/语义三层记忆系统，支持跨会话个性化应答与多用户隔离。
通过 asyncio 并行检索使检索耗时降低 35%，双源检索平均响应 < 8s；FastAPI + SSE 与 Vue3 实时展示执行进度，端到端研报生成由小时级缩短至半小时内。

真实数据包（只允许使用这些数字，或按「待复测」标注）：
[把上面的「真实数据包」表格贴进来]

改写要求：
1. 项目介绍保留产品价值（企业级 AI 深度研究助手、多 Agent 流水线），但每个数字后面必须带口径或样本量。
2. 新增至少 3 条「测试与质量保障」职责，格式统一为：动作 + 方法 + 数值 + 口径。参考示例：
   - 搭建三层评测体系：自建 10 条带标签查询集，封装 LangGraph 内部评测、FastAPI 黑盒评测、HTTP 并发压测脚本，一键复跑，当前 10/10 用例通过、0 失败。
   - 建立指标口径与自动统计脚本，统一计算意图路由准确率、引用有效性、完备性、延迟分位数；样例集实测意图路由准确率 100%（10/10），平均端到端耗时 58.8s（P50 93.8s）。
   - 设计「LLM 初筛 + 人工盲评」双评审流程，生成逐条评审表，幻觉率与完备性以人工复核为准。
   - 通过 Analyst + Reflect 迭代补搜闭环，50% 用例触发补搜、平均识别并补齐 1.9 个信息缺口。
3. 以下数字如果仓库没实测，禁止写「达成/提升至」：96%、45%→12%、62%→89%、25%→6%、94%、35%、<8s。可以写成「原方案目标值」或直接删掉。
4. 把当前测试缺口写成「测试发现的问题」类成果，例如「通过评测暴露本地检索证据留存率为 0、引用数为 0 的问题，推动定位与修复」；如果你不知道根因，标注「待补充根因」，不要编造。
5. 输出格式：项目介绍（2-3 行）+ 项目职责（5-6 条，其中测试至少 3 条）+ 成果要点（3-4 条）+ 面试官可能追问的 5 个问题及诚实回答要点。
6. 全文中文，简历语言，不用营销词，不写「200 条」。
```

## 四、提示词 C：面试追问准备

```
你是我，正在面试。基于以下真实情况，准备 5 个高频问题的回答：
Q1: 你这套测试体系是怎么设计的？
Q2: 为什么只有 10 条样本？准确率 100% 是不是样本太小？
Q3: 简历里 96%、94%、6% 这些数字是哪来的？
Q4: 黑盒评测和压测具体怎么跑的？结果文件在哪？
Q5: 评测发现本地检索证据全部被丢弃（raw 22 / kept 0），你怎么解释和修复？

每个回答遵循：先讲测试口径 → 再讲数字 → 能引用文件或命令 → 没实测的明确说「这是原文档口径，计划扩到 200 条后复测」。不要背稿，控制在 1 分钟内。
```

## 五、可选：提示词 D 扩到 200 条再复测

如果你愿意花时间把数字做扎实，先跑这个：

```
把 app/eval/sample_queries.jsonl 从 10 条扩到 200 条：覆盖 direct（约 20%）和 multiagent（约 80%），业务场景覆盖行业调研、竞品分析、政策解读、技术选型；每条必须包含 id、query、expected_route、expected_sub_questions。
扩完后按 eval_run.ps1 -Action all 复跑，人工盲评至少 20% 样本，把 output/eval_metrics.json 的实测数字更新回简历。
```

## 六、使用顺序

1. 先跑提示词 A（或 D）拿到最新真实数字；
2. 把「真实数据包」更新成最新结果，再用提示词 B 改写简历；
3. 用提示词 C 准备面试，确保每个数字都能讲出口径和来源。
