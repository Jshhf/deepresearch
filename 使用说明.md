# DeepResearch 启动与使用说明

适用项目目录：`D:\deepresearch\deep_research`
部署状态文件：`D:\deepresearch\deploy-progress.md`

## 1. 日常启动

1. 启动 Docker Desktop：开始菜单搜索 Docker Desktop，或运行 `D:\Docker\Docker Desktop.exe`。
2. 等托盘图标变绿，确认引擎可用：

```powershell
docker version
```

3. 在项目根目录启动全部服务：

```powershell
cd D:\deepresearch\deep_research
docker compose --env-file .env.docker up -d
```

4. 确认状态：

```powershell
docker compose --env-file .env.docker ps
```

期望 `backend`、`frontend`、`postgres`、`redis`、`etcd`、`minio`、`milvus-standalone` 全部为 `Up`，带健康检查的显示 `healthy`。

## 2. 访问地址

| 服务 | 地址 |
| --- | --- |
| 前端工作台 | http://localhost:8080 |
| 后端 API 文档 | http://localhost:8000/docs |
| 后端健康检查 | http://localhost:8000/health |
| MinIO 控制台 | http://localhost:9001 （账号 `minioadmin`，密码见 `.env.docker`） |
| Milvus 管理界面 Attu（可选） | http://localhost:8001 |

Attu 默认不随全栈启动，需要单独启动：

```powershell
cd D:\deepresearch\deep_research
docker compose --env-file .env.docker --profile tools up -d attu
```

停止 Attu：

```powershell
docker compose --env-file .env.docker --profile tools stop attu
```

## 3. 健康检查

```powershell
curl http://localhost:8000/health
curl -I http://localhost:8080
docker compose --env-file .env.docker exec postgres pg_isready -U deepresearch -d deepresearch
docker compose --env-file .env.docker exec redis redis-cli -a deepresearch ping
curl http://localhost:9091/healthz
docker compose --env-file .env.docker exec etcd etcdctl endpoint health
curl http://localhost:9000/minio/health/live
```

## 4. 停止与重启

```powershell
# 停止全部服务（保留数据卷）
docker compose --env-file .env.docker down

# 重新启动
docker compose --env-file .env.docker up -d

# 只重启后端（改配置后推荐用下面第 5 节的 force-recreate 方式）
docker compose --env-file .env.docker restart backend

# 删除所有数据卷（慎用，会清空数据库和向量数据）
docker compose --env-file .env.docker down -v
```

## 5. 更换 API Key

Key 只保存在 `D:\deepresearch\deep_research\.env.docker`，不要写入源码或提交仓库。

1. 编辑 `.env.docker`，替换 `DASHSCOPE_API_KEY=` 后面的值。
2. 重建后端容器（`restart` 不会重新读取 Key，必须重建）：

```powershell
cd D:\deepresearch\deep_research
docker compose --env-file .env.docker up -d --force-recreate backend
```

3. 验证：

```powershell
curl http://localhost:8000/health
```

## 6. 文档入库（RAG 本地知识库）

1. 把 `.md` / `.txt` / `.markdown` 文件放入 `docs/`（支持子目录）。
2. 执行：

```powershell
cd D:\deepresearch\deep_research
docker compose --env-file .env.docker --profile ingest run --rm ingest
```

3. 看到 `入库完成 | 文件数=N | chunk数=M | collection=mult_agent_memory` 即成功。

## 7. 查看日志

```powershell
docker compose --env-file .env.docker logs -f backend
docker compose --env-file .env.docker logs -f milvus-standalone
docker compose --env-file .env.docker logs -f postgres
```

完整部署日志：`D:\deepresearch\deploy.log`

## 8. 数据位置

| 数据 | 位置 |
| --- | --- |
| Docker Desktop 程序 | `D:\Docker` |
| Docker/WSL 数据根 | `D:\DockerData` |
| 项目输出（研究报告等） | `deep_research\output` |
| SQLite 记忆库 | `deep_research\app\data` |
| 待入库文档 | `deep_research\docs` |
| PostgreSQL/Redis/etcd/MinIO/Milvus 数据 | Docker 命名卷（实际 VHD 位于 `D:\DockerData`） |

## 9. 一键部署 / 自检脚本

`D:\deepresearch\deploy.ps1` 可重复执行，包含环境变量校验、构建启动、健康检查、文档入库；脚本会保留 `.env.docker` 中已有的 API Key，不会用旧环境变量覆盖。

```powershell
powershell -ExecutionPolicy Bypass -File D:\deepresearch\deploy.ps1
```

## 10. 常见问题

- Docker 命令找不到：新开 PowerShell，或先刷新 PATH：`$env:Path = [Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [Environment]::GetEnvironmentVariable('Path','User')`。
- 6379 端口冲突：本机 Windows Redis 服务已停止并改为手动启动；若它再次运行，用管理员 PowerShell 执行 `Stop-Service Redis` 后再启动容器。
- 后端健康但提问报错：先看 `docker compose --env-file .env.docker logs backend`，按第 5 节确认 Key 是否有效。
- 向量入库失败：确认 `docs/` 有文件、Milvus healthy、`DASHSCOPE_API_KEY` 有效。
